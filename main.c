#include "ft_printf.h"

#include <time.h>

 void test_main();

int main()
{
	 test_main();

	//char	*c = NULL;
	 //int i = 0;
	// char *cmd = "{x = je suis petit %12X}" ;
	// // char *cmd[5] = {"{%ll20.15d}", "{%l20.15d}", "{%20.15d}", "{%h20.15d}", "{%hh20.15d}"};
	 // int value = -73572;
	// char c = '\0';
	// printf(" -> THEM : [%d]\n", printf(cmd, value));
	// printf("Test with %s vs %c\n", cmd, c);
	// ft_printf(" ->  OUR : [%d]\n", ft_printf(cmd, value));
	return (0);
}
